@attribute [InputFieldControl(typeof(bool), IsDefault = false)]

<input type="checkbox" @onchange="e => ValueChanged.InvokeAsync(Value = (bool)e.Value)" checked="@Value" />

@code {
    [Parameter] public bool Value { get; set; }
    [Parameter] public EventCallback<bool> ValueChanged { get; set; }
}
